#!/bin/sh
# Same entry point for .deb, tarball, IBus and editor clients.
set -eu
PREFIX="$(CDPATH='' cd "$(dirname "$0")/.." && pwd)"
exec python3 "$PREFIX/share/geist-diktat/launcher.py" --prefix "$PREFIX" "$@"
